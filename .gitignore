# Environment
.env

# User config (use config.yml.sample as template)
config.yml

# Syncable data (directories and files)
.openclaw/
workspaces/
# symlinked from other locations, so not ignored by default
.openclaw
workspaces
